// A nested loop is used when we want to execute a block of code multiple times, and each iteration of the
// outer loop can have its own inner loops.
#include <stdio.h>

int main() {
    int i = 0;
    int j;

    // Keep printing numbers from 0 to 4 for each number from 0 to 4.
    for (i = 0; i < 5; i++) {
        printf("Number %d: ", i);

        // For each outer loop iteration, print the current number 5 times.
        for (j = 0; j < 5; j++) {
            printf("%d ", j);
        }

        printf("\n");
    }

    for (i = 0; i < 5; i++) {
        printf("Number %d: ", i);

        // For each outer loop iteration, print the current number 5 times.
        for (j = 0; j < 5; j++) {
            printf("%d ", j);
        }

        printf("\n");
    }

    return 0;

        // For each outer loop iteration, print the current number 5 times.
        for (j = 0; j < 5; j++) {
            printf("%d ", j);
        }

        printf("\n");
    }

        for (j = 0; j < 5; j++) {
            printf("%d ", j);
        }

        printf("\n");
    }

        }

        printf("\n");
    }


        printf("\n");
    }

    }


    return 0;
}
    return 0;
}