#include <stdio.h>

int main() {
    // Assign a value directly to the earnings variable
    float earnings = 4500.75;

    // Print out the earnings
    printf("You have earned: %.2f\n", earnings);

    return 0;
}
